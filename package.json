{
  "name": "studystreak",
  "version": "0.3.0",
  "description": "StudyStreak — consistency-first study planner for JEE droppers. AWS App Runner + RDS Postgres.",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "node server/server.js",
    "dev": "node --watch server/server.js",
    "test": "node --test test/streak.test.js test/priority.test.js test/reward.test.js"
  },
  "dependencies": {
    "fastify": "^4.27.0",
    "@fastify/static": "^7.0.4",
    "@fastify/cors": "^9.0.1",
    "pg": "^8.13.1"
  },
  "engines": {
    "node": ">=18"
  }
}
