{
  "name": "studystreak",
  "version": "0.4.0",
  "description": "StudyStreak — consistency-first study planner for JEE droppers. Vercel-deployable.",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "test": "node --test test/streak.test.js test/priority.test.js test/reward.test.js",
    "build": "echo 'static-only build; api/* deployed as serverless functions'"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.1"
  },
  "devDependencies": {
    "vercel": "^39.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
