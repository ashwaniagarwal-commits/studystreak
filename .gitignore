node_modules/
data/
*.db
*.db-shm
*.db-wal
.env
.env.local
.vercel
.DS_Store
/dist
/.next

# Vestigial folders from the local-only v0.1 (Fastify + SQLite). Safe to delete
# from your working copy. Not deployed by Vercel either way (api/ + public/ are
# the only directories Vercel uses).
server/
web/
scripts/
