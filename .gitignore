node_modules/
data/
*.db
*.db-shm
*.db-wal
.env
.env.local
.vercel
.DS_Store
/dist
/.next

# Vestigial AWS files (kept in repo for future Path C migration). Vercel ignores them.
# server/
# Dockerfile
# .dockerignore
# apprunner.yaml
# AWS_DEPLOY.md
